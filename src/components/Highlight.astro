---
interface Props {
    variant?: "primary" | "alternate" | "blue" | "purple" | "green";
}

const { variant = "primary" } = Astro.props;

const styles = {
    primary: "font-bold text-[var(--sl-color-accent-high)]",
    alternate:
        "block p-4 bg-[var(--sl-color-accent-low)] bg-opacity-30 border-l-4 border-[var(--sl-color-accent)] rounded my-4",
    blue: "font-bold text-[var(--blue)]",
    purple: "font-bold text-[var(--purple)]",
    green: "font-bold text-[var(--green)]",
};

const className = styles[variant] || styles.primary;
---

<span class={className}>
    <slot />
</span>
